<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Material.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="TitleFont" parent="@android:style/TextAppearance.Large">
        <item name="android:textColor">@color/title_colour</item>
        <item name="android:typeface">normal</item>
    </style>

    <style name="AppButton" parent="Base.Widget.AppCompat.Button">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:elevation">16dp</item>
        <item name="android:background">@color/apps_button</item>
        <item name="android:layout_marginTop">12dp</item>
    </style>
</resources>
